version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install --global yarn
      - yarn

  pre_build:
    commands:
      - yarn test -- --watchAll=false

  build:
    commands:
      - yarn build

cache:
  paths:
    - 'node_modules/**/*'

artifacts:
  files:
    - "**/*"
  base-directory: build
